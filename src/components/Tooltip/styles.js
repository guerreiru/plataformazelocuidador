import styled from 'styled-components/native';
import colors from 'styles/colors';

export const Info = styled.View``;

export const Popover = styled.SafeAreaView`
  flex: 1;
  justify-content: center;
`;

export const Message = styled.Text`
  color: ${colors.TITLE};
  flex-shrink: 1;
`;
